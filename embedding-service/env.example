# Nombre del modelo de embeddings (debe ser el MISMO que usaste para
# generar los vectores ya guardados en tu tabla `documents`, o tendrás
# que re-embeber todo el corpus).
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-base
